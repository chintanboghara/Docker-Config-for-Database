# Database data directories for Docker Compose persistent storage
# These directories serve as bind mounts for database containers, storing large data files
# that should not be tracked in version control.

# MongoDB data directory (bind mount for container's /data/db)
mongodb/db_data/

# MySQL data directory (bind mount for container's /var/lib/mysql)
mysql/mysql/

# PostgreSQL data directory (bind mount for container's /var/lib/postgresql/data)
postgresql/data/

# Optional: Uncomment the following if using bind mounts for Redis persistence
# redis/data/          # Redis data directory
# redisinsight/db/     # RedisInsight data directory
